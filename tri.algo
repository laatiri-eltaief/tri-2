ALGORITHM sort
VAR
    i:integer;
    arr[i]:integer;
    j:integer;
BEGIN
read arr [n]
    FOR i FROM 0 TO i-1  DO
        j=i-1
        WHILE (j>=0 && arr[j]>arr[i]) DO
            arr[j+1]=arr[j];
            j--;
        END_WHILE
        arr[j+1]=arr[i];
    END_FOR
    return array;
END